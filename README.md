To run the app:

1. Install the dependencies:

```bash
cd front/
npm install
```

2. Run the frontend:

```bash
npm run dev
```

3. Run the backend:

```bash
cd back/
npm i
docker compose build
docker compose up
```
